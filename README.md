# image procress

Repository for image processing and camera capture project.

Notes:
- `ProcessFolder/` is ignored and will not be included in the repo (see `.gitignore`).
- Adjust `.gitignore` if you want to track any folders currently excluded.

How to push to GitHub:
1. Create a repository named `image procress` on GitHub (web or use `gh repo create`).
2. Add the GitHub remote and push:

   git remote add origin <remote-url>
   git push -u origin main
